import { Pricing } from "@/components/marketing/Pricing"

export const metadata = {
  title: "Pricing | Queuely",
  description: "Simple, transparent pricing for notification infrastructure.",
}

export default function PricingPage() {
  return <Pricing />
}
